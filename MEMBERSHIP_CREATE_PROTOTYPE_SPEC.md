# 멤버십 생성 프로토타입 v3 구현 스펙

## 1. 문서 개요

| 항목 | 내용 |
| --- | --- |
| 문서 목적 | 멤버십 생성 프로토타입 v3의 화면, 동작, 계산 규칙, 유효성 검증 및 미구현 정책을 정리함 |
| 기준일 | 2026-09-16 |
| 대상 파일 | \`membership-create-prototype/index-ver3.html\` |
| 적용 범위 | v3 멤버십 생성 화면 |
| 제외 범위 | \`index.html\`, \`index-ver2.html\`, 실제 저장·결제·권한 API |
| 기술 구조 | 단일 HTML, CSS, Vanilla JavaScript |
| 배포 | Cloudflare Pages \`membership-create-prototype\` / \`https://membership-create-prototype.pages.dev/index-ver3.html\` |

## 2. 구현 원칙

- 다크 테마, 카드형 Radio UI, 아코디언 구조를 유지함
- 사용자 노출 문구는 \`-습니다\`체를 기본으로 사용함
- 긍정·활성 선택지를 먼저 배치하되, 기본값은 별도로 관리함
- 운영 방식을 먼저 선택하고, 해당 방식에 필요한 하위 설정만 노출함
- 입력값과 가격 요약, 우측 요약 패널, 미리보기를 실시간으로 동기화함
- 서버 저장 없이 브라우저 안에서만 동작하는 프로토타입임

## 3. 화면 정보 구조

~~~text
1. 멤버십 운영 방식
   └─ 가입일 기준 / 종료일 기준 / 혜택 이용 기준

2. 멤버십 설정
   ├─ 2-1. 멤버십 정보 및 가격
   ├─ 2-2. 이용 및 갱신 설정 또는 종료일 및 모집 설정
   ├─ 2-3. 가입 설정
   └─ 2-4. 공개 상태

3. 혜택 설정
   ├─ 3-1. 기본 혜택
   └─ 3-2. 추가 혜택

4. 상세 소개

우측 요약 패널
   └─ 생성 유형, 입력 진행률, 가격, 미완료 항목, 미리보기, 생성 CTA
~~~

## 4. 운영 방식

### 4.1 공통

- 운영 방식 제목 아래에 다음 안내를 노출함.

  > 멤버십 이용이 언제 끝날지를 먼저 정합니다.

- 선택 전에는 하위 폼을 숨김.
- 작성 내용이 있는 상태에서 다른 운영 방식을 선택하면 확인 팝업을 노출함.

  > 멤버십 운영 방식을 변경하면 입력한 내용이 모두 삭제됩니다.
  > 운영 방식을 정말 변경하시겠어요?

  - \`취소\`, 바깥 클릭, Escape: 기존 입력 유지
  - \`변경\`: 입력값·혜택·상세 소개·선택 상태를 초기화한 뒤 새 방식을 적용

### 4.2 선택지와 확정 유형명

| 운영 방식 | 카드 설명 | 하위 설정 | 확정 유형명 |
| --- | --- | --- | --- |
| 가입일 기준 | 가입한 날부터 설정한 기간 동안 이용합니다. 회원마다 종료일이 다릅니다. | 기본 이용 기간, 갱신 방식, 조건부 묶음 이용권 | 자동 갱신: 정기 구독 멤버십 / 1회 이용: 단건 이용 멤버십 · N개월권 |
| 종료일 기준 | 가입일과 관계없이 지정한 날에 모든 회원의 이용이 함께 종료됩니다. | 이용 종료일, 모집 기간 | 기간 한정 멤버십 · YYYY-MM-DD 종료 |
| 혜택 이용 기준 | 제공한 혜택을 모두 사용하면 이용이 종료됩니다. | 기본 이용 기간, 갱신 방식 | 혜택 소진 멤버십 · 정기 갱신 또는 단건 |

- 확정 유형명은 필요한 설정이 완료되었을 때 우측 요약 패널과 미리보기에 노출함.
- 보조 문구는 \`이 조합으로 생성됩니다.\`를 사용함.

## 5. 멤버십 정보와 가격

### 5.1 정가

| 항목 | 필수 | 규칙 |
| --- | --- | --- |
| 멤버십 이름 | 필수 | 최대 60자 |
| 한 줄 설명 | 필수 | 최대 400자 |
| 대표 이미지 | 필수 | 화면 안내: 100px × 100px 이상, 10MB 이하 |
| 정가 | 필수 | 1,000원 이상 5,000,000원 이하의 정수 |

- 판매가는 직접 입력하지 않으며 판매가 할인값으로 자동 산출함.

### 5.2 할인 입력

| 구분 | 입력 방식 | 기본 모드 | 적용 기준 |
| --- | --- | --- | --- |
| 판매가 할인 | 정률(%) 또는 정액(원) | 정률 | 매 결제 |
| 첫 결제 할인 | 정률(%) 또는 정액(원) | 정률 | 최초 1회 |

- 두 할인은 선택 입력임. 할인값을 비워두면 적용하지 않음.
- 정률은 1~99의 정수만 허용함. 소수점 입력은 오류 처리함.
- 정액은 1원 이상의 정수만 허용함.
- 첫 결제 할인은 가입일 기준 멤버십에서만 노출함.

### 5.3 계산식

~~~text
판매가 할인 금액 = 정률이면 정가 × 판매가 할인율 / 100, 정액이면 입력 금액
첫 결제 할인 금액 = 정률이면 정가 × 첫 결제 할인율 / 100, 정액이면 입력 금액

판매가 = floor100(정가 - 판매가 할인 금액)
첫 결제 금액 = floor100(정가 - 판매가 할인 금액 - 첫 결제 할인 금액)
~~~

- \`floor100(x)\`은 100원 단위 내림임.
- 첫 결제 금액은 판매가에서 다시 빼지 않고 정가에서 두 할인 금액을 한 번에 뺀 뒤 절사함.
- 정률 입력 시 표시 할인율은 입력값을 유지함.
- 정액 입력 시 표시 할인율은 절사 후 실제 차감액을 정가로 나눈 값의 소수점 이하를 버려 계산함.
- 총 할인율은 판매가 할인율과 첫 결제 할인율을 더해 표기함. 최종 금액에서 역산하지 않음.

### 5.4 가격 요약과 유효성 검증

가격 요약은 다음 규칙으로 표시함.

~~~text
정가
판매가 할인 N% / -할인 금액       (할인이 있을 때만)
판매가
첫 결제 할인 N% / -할인 금액      (할인이 있을 때만)
첫 결제 금액 · 정가 대비 총 N% 할인 (첫 결제 할인이 있을 때만)
2회차부터 · 정가 대비 판매가 할인율 (가입일 기준 자동 갱신 + 첫 결제 할인이 있을 때만)
~~~

- 할인값이 0인 행은 렌더링하지 않음.
- 판매가와 첫 결제 금액은 모두 1,000원 이상이어야 함.
- 정률 상한은 정가·판매가 할인 입력 상태에 따라 다시 계산해 입력칸 아래에 노출함.
- 조건을 충족하지 않으면 할인 설정을 미완료 항목으로 처리하고 생성 CTA를 비활성화함.
- 판매가 할인의 100원 절사로 실제 할인율이 입력값보다 1%p 이상 커지면 아래 문구를 표시함.

  > 100원 단위 절사로 실제 할인율은 N%가 됩니다.

## 6. 이용·갱신·모집 설정

### 6.1 가입일 기준과 혜택 이용 기준

- 기본 이용 기간은 1개월 이상 입력해야 함.
- 기본 이용 기간 아래에 다음 안내를 표시함.

  > 가입한 회원이 멤버십을 사용할 수 있는 기간입니다.

| 갱신 방식 | 안내 |
| --- | --- |
| 자동 갱신, 첫 결제 할인 없음 | 기본 이용 기간이 끝나면 자동으로 결제되며, 같은 기간만큼 이용 기간이 연장됩니다. 결제 금액과 제공 혜택은 최초 결제와 동일합니다. |
| 자동 갱신, 첫 결제 할인 있음 | 기본 이용 기간이 끝나면 자동으로 결제되며, 같은 기간만큼 이용 기간이 연장됩니다. 첫 결제 할인은 최초 1회만 적용되므로, 이후 결제 금액은 판매가입니다. 제공 혜택은 최초 결제와 동일합니다. |
| 1회 이용 | 기본 이용 기간이 끝나면 자동 갱신 없이 이용이 종료됩니다. |

- 가입일 기준에서 기본 이용 기간이 1개월이고 자동 갱신일 때만 묶음 이용권을 노출함.
- 묶음 이용권 판매 시 3·6·12개월권을 선택할 수 있음.
- 묶음 이용권으로 가입한 회원은 구매한 기간이 끝나면 자동 갱신 없이 종료됨.

### 6.2 혜택 이용 기준 만료 안내

- 혜택 이용 기준에서만 아래 경고 안내를 표시함.

  > 설정한 이용 기간이 끝나면 제공한 혜택이 남아 있어도 멤버십 이용이 종료됩니다.

- 자동 갱신이면 아래 문장을 추가함.

  > 자동 갱신 시 남은 혜택은 이월되지 않으며, 새 기간의 혜택이 다시 제공됩니다.

### 6.3 종료일 기준과 모집 기간

- 이용 종료일은 필수임.
- 모집 기간은 상시 모집 또는 특정 기간 모집을 선택함.
- 특정 기간 모집을 선택하면 모집 시작일·종료일을 노출함.
- 모집 종료일은 모집 시작일보다 빠를 수 없고 이용 종료일보다 늦을 수 없음.
- 모집 기간 아래에 다음 안내를 표시함.

  > 새로 가입을 받을 수 있는 기간입니다. 이용 기간과 다릅니다.

## 7. 가입·공개 설정

- 가입 인원은 제한 있음 또는 제한 없음으로 설정함.
- 제한 있음이면 최대 가입 인원을 노출하며 1~9,999명 정수만 허용함.
- 가입 승인 방식은 자동 승인과 직접 승인 중 선택함.
- 공개 상태는 공개, 일부 공개, 나에게만 공개를 독립 선택지로 제공함.

## 8. 혜택 설정

### 8.1 기본 혜택

- 다이렉트 메시지 이용 권한과 멤버십 배지를 설정할 수 있음.
- 기본 혜택의 제공함·제공하지 않음 선택 카드에는 부가 설명을 두지 않음.
- 다이렉트 메시지 수신 대상은 없음, 모든 팬, 모든 멤버, 특정 멤버십 가입자로 설정함.
- 특정 멤버십 가입자에는 생성중인 멤버십 포함과 대상 멤버십 선택을 제공함.
- 배지는 기본 배지 또는 커스텀 배지로 설정함.
- 커스텀 배지는 PNG/JPG, 1MB 미만, 최소 32 × 32px, 1:1 이미지만 허용함.

### 8.2 추가 혜택

- 기존 리워드를 현재 멤버십에 연결해 제공함.
- 혜택을 새로 생성하면 즉시 현재 멤버십에 연결함.
- 각 연결 혜택에서 수정, 위로 이동, 아래로 이동, 이 멤버십에서 제거를 제공함.
- 첫 항목의 위로 이동과 마지막 항목의 아래로 이동은 비활성화함.
- 수정은 기존 위치를 유지하고 이름(필수, 40자 이내)·설명(선택, 100자 이내)을 변경함.
- 공용 리워드를 수정하면 다른 멤버십에도 같은 변경이 반영된다는 안내 팝업을 노출함.
- 미리보기 혜택 태그는 현재 목록 순서를 그대로 따름.

## 9. 요약 패널과 미리보기

- 필수 항목 완료 수, 미완료 필드 이동, 생성 가능 상태를 실시간 갱신함.
- 요약 패널에는 확정 유형명, 이용 기간, 갱신 방식, 정가, 판매가, 첫 결제 금액, 추가 혜택 수를 표시함.
- 미리보기의 가격 영역은 정가 취소선, 총 할인율 배지, 첫 결제 금액, 자동 갱신일 때 2회차부터 판매가를 보여줌.
- 회원 노출 영역에는 개별 할인율이 아니라 총 할인율 하나만 표시함.
- 미리보기 CTA는 상태 확인용이며 실제 구매·저장을 수행하지 않음.

## 10. 생성 처리

- CTA 문구는 \`멤버십 생성하기\`를 사용함.
- 모든 필수 입력과 가격 유효성 검증을 통과해야 활성화함.
- 클릭하면 \`멤버십이 생성되었습니다.\` 토스트만 표시함.
- 실제 멤버십 생성, 결제 생성, 권한 지급 API는 연결하지 않음.

## 11. 접근성과 반응형

- Radio, 체크박스, 모달, 아코디언에 적절한 역할·상태 속성을 유지함.
- 모달은 닫기 버튼, 바깥 클릭, Escape로 닫을 수 있음.
- 날짜 입력 영역 전체 클릭 시 브라우저 기본 달력 선택기를 호출함.
- 화면 폭 940px 이하에서는 요약 패널을 본문 위로 이동하고, 620px 이하에서는 입력 그리드를 1열로 전환함.

## 12. 범위 제외 및 결정 필요 항목

| ID | 내용 | 현 상태 |
| --- | --- | --- |
| WI-09 | 멤버십별 DM 설정과 크리에이터 메시지 설정의 우선순위 | 정책 결정 전으로 구현하지 않음 |
| D-01 | 혜택 이용 기준 만료 시 잔여 혜택 소멸·이월·환불 | 프로토타입 안내 문구만 적용 |
| D-03 | 정가를 100원 단위로 강제할지 | 정수 입력만 강제 |
| D-04 | 추가 혜택 수정이 원본 리워드에 미치는 범위 | 공용 리워드 수정 안내만 적용 |

## 13. 검증 기준

- 정가 79,900원, 판매가 할인 25%, 첫 결제 할인 15%: 판매가 59,900원, 첫 결제 금액 47,900원, 총 40% 표시
- 정가 79,900원, 판매가 할인 20,000원, 첫 결제 할인 15%: 판매가 59,900원, 첫 결제 금액 47,900원
- 정가 79,900원, 판매가 할인 30,000원: 판매가 49,900원
- 정가 3,000원에서 첫 결제 총 할인으로 900원 미만이 되면 생성 불가 및 상한 안내 노출
- 할인율 소수점 입력 시 오류 처리
- 가입일 기준 + 자동 갱신 + 1개월에서만 묶음 이용권 노출
- 종료일 기준의 특정 기간 모집에서 날짜 선후 관계 오류 처리
- 추가 혜택의 순서를 바꾸면 미리보기 순서도 동일하게 변경

## 14. 변경 이력

| 일자 | 내용 |
| --- | --- |
| 2026-09-03 | v1 멤버십 생성 프로토타입 최초 구현 |
| 2026-09-04 | 운영 방식별 하위 설정, 종료일 기준 모집 기간, 날짜 유효성, 미리보기 개선 |
| 2026-09-15 | v3 확정 유형명, 혜택 만료 안내, 리워드 연결·수정·순서 변경, 배지·DM 설정 반영 |
| 2026-09-16 | v3 가격 입력을 정가·판매가 할인·첫 결제 할인 자동 산출 구조로 개편하고 관련 문구·검증·미리보기를 갱신 |
