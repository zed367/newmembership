# 멤버십 플랜 생성 프로토타입 구현 스펙

## 1. 문서 개요

| 항목 | 내용 |
| --- | --- |
| 문서 목적 | 멤버십 생성 프로토타입(v1)의 현재 화면, 동작, 유효성 규칙 및 변경 이력을 한 문서에 정리함 |
| 기준일 | 2026-09-15 |
| 대상 파일 | `membership-create-prototype/index.html` |
| 적용 범위 | v1 멤버십 생성 화면 |
| 제외 범위 | `index-ver2.html` 및 실제 멤버십 저장·결제·백엔드 연동 |
| 기술 구조 | 단일 HTML, CSS, Vanilla JavaScript |
| 배포 | Cloudflare Pages `membership-create-prototype` / https://membership-create-prototype.pages.dev |

## 2. 구현 원칙

- 다크 테마와 카드형 Radio UI를 유지함
- 사용자 노출 문구는 `~습니다` 체를 기본으로 사용함
- 긍정/활성 선택지를 좌측 또는 먼저 배치하고, 기본값은 별도로 명시함
- 운영 방식을 먼저 선택하고, 선택한 방식에 필요한 하위 설정만 노출함
- v1과 v2를 분리함. 본 문서와 현재 배포 대상은 v1임
- 화면 상태와 계산은 브라우저 내 JavaScript로 처리하며, 서버 저장은 구현하지 않음

## 3. 화면 정보 구조

```text
1. 멤버십 운영 방식
   └─ 가입일 기준 / 종료일 기준 / 혜택 이용 기준

2. 멤버십 설정
   ├─ 2-1. 멤버십 정보 및 할인
   ├─ 2-2. 이용 및 갱신 설정 또는 종료일 및 모집 설정
   ├─ 2-3. 가입 설정
   └─ 2-4. 공개 상태

3. 혜택 설정
   ├─ 3-1. 기본 혜택
   └─ 3-2. 추가 혜택

4. 상세 소개

우측 요약 패널
   └─ 입력 진행률, 미완료 필수 항목, 미리보기, 등록 버튼
```

## 4. 운영 방식

### 4.1 선택지

| 운영 방식 | 설명 | 노출 설정 |
| --- | --- | --- |
| 가입일 기준 | 가입한 날부터 설정한 기간 동안 이용함 | 이용 기간, 갱신 방식, 조건부 묶음 이용권 |
| 종료일 기준 | 가입일과 관계없이 지정한 이용 종료일에 이용이 끝남 | 이용 종료일, 모집 기간 |
| 혜택 이용 기준 | 가입 시 제공된 혜택을 모두 사용하면 이용이 종료되는 방식임 | 이용 기간, 갱신 방식. 세부 이용 횟수·종료 정책 입력은 미구현 |

### 4.2 선택 및 전환 규칙

- 최초에는 운영 방식을 선택할 때까지 하위 폼 전체를 숨김
- 같은 운영 방식을 다시 선택하면 아무 동작을 하지 않음
- 운영 방식을 선택한 뒤 작성 내용이 없으면 다른 방식으로 즉시 전환함
- 작성 내용이 있으면 아래 확인 팝업을 표시함

```text
멤버십 운영 방식을 변경하면 입력한 내용이 모두 삭제됩니다.
운영 방식을 정말 변경하시겠어요?
[취소] [변경]
```

- `취소`, 팝업 바깥 클릭, Escape 키 입력 시 기존 데이터를 유지함
- `변경` 선택 시 아래 항목을 초기화한 뒤 새 운영 방식을 적용함
  - 멤버십 이름, 한 줄 설명, 판매가, 기간·날짜, 대표 이미지
  - 즉시 할인·첫 결제 할인 값과 선택값
  - 갱신 방식, 묶음 이용권, 모집 기간, 가입 인원, 가입 승인, 공개 상태
  - 기본 혜택, 추가 혜택, 상세 소개

## 5. 멤버십 정보 및 판매 조건

### 5.1 기본 정보

| 항목 | 필수 | 규칙 |
| --- | --- | --- |
| 멤버십 이름 | 필수 | 최대 60자 |
| 한 줄 설명 | 필수 | 최대 400자 |
| 대표 이미지 | 필수 | 화면 안내: 100px × 100px 이상, 10MB 이하. 파일 크기 10MB 초과 시 선택을 막음 |
| 판매가 | 필수 | 1,000원 이상 5,000,000원 이하 |

- 대표 이미지를 선택하면 파일명과 이미지를 즉시 표시함
- 입력 글자 수와 필수 항목 완료 수를 실시간으로 갱신함

### 5.2 즉시 할인

| 항목 | 내용 |
| --- | --- |
| 선택지 | 적용 / 적용 안 함 |
| 기본값 | 적용 안 함 |
| 입력 방식 | 원 단위 정액 할인 |
| 입력 규칙 | 1원 이상, 판매가 이하 |
| 계산 | `즉시 할인 적용가 = 판매가 - 즉시 할인 금액` |
| 노출 | 할인 적용가와 할인 금액을 실시간으로 표시 |

### 5.3 첫 결제 할인

가입일 기준 멤버십에서만 노출합니다. 종료일 기준과 혜택 이용 기준에서는 숨깁니다.

| 선택지 | 입력값 | 유효성 | 계산 |
| --- | --- | --- |
| 정액 할인 | 원 단위 할인 금액 | 1원 이상, 즉시 할인 적용가 이하 | `첫 결제 적용가 = 즉시 할인 적용가 - 정액 할인 금액` |
| 정률 할인 | 정수 할인율(%) | 1% 이상 100% 이하 | `할인 금액 = floor(즉시 할인 적용가 × 할인율 / 100)` |
| 적용 안 함 | 입력 없음 | 해당 없음 | 첫 결제 할인을 적용하지 않음 |

- 첫 결제 할인의 계산 기준은 즉시 할인 적용가임
- 정률 할인으로 계산된 원 단위 미만 금액은 버림(`floor`) 처리함
- 정률 할인 선택 시 `할인율은 즉시 할인 적용가를 기준으로 계산됩니다.`를 안내함
- 할인 결과 영역에는 첫 결제 적용가와 실제 원화 할인액을 함께 표시함

### 5.4 할인 입력 오류 처리

- 판매가가 유효하지 않으면 할인 입력값을 계산하지 않고 판매가 입력을 안내함
- 즉시 할인 금액이 유효하지 않으면 첫 결제 할인 계산을 막고 즉시 할인 금액 확인을 안내함
- 정액 할인 오류: 즉시 할인 적용가 이하의 1원 이상만 허용함
- 정률 할인 오류: 1% 이상 100% 이하의 정수만 허용함

## 6. 운영 방식별 이용·모집 설정

### 6.1 가입일 기준 및 혜택 이용 기준

| 항목 | 규칙 |
| --- | --- |
| 기본 이용 기간 | 필수. 입력 UI는 1~120개월 범위를 안내하며, 완료 판정은 1개월 이상임 |
| 갱신 방식 | 자동 갱신 / 1회 이용. 기본값은 자동 갱신 |
| 자동 갱신 설명 | 기간 종료 후 동일 기간으로 자동 결제되어 계속 이용함 |
| 1회 이용 설명 | 기간 종료 후 자동 갱신 없이 종료됨 |

### 6.2 묶음 이용권

| 조건 | 동작 |
| --- | --- |
| 가입일 기준 + 기본 이용 기간 1개월 + 자동 갱신 | 묶음 이용권 판매 설정을 노출함 |
| 가입일 기준 + 1회 이용 | 묶음 이용권을 노출하지 않음 |
| 가입일 기준 + 이용 기간 2개월 이상 | 묶음 이용권을 노출하지 않음 |
| 종료일 기준 또는 혜택 이용 기준 | 묶음 이용권을 노출하지 않음 |

- 판매 / 판매 안 함 중 선택하며 기본값은 `판매 안 함`임
- 판매 선택 시 3개월권, 6개월권, 12개월권을 표시함
- 안내 문구: 구매한 묶음 이용권 기간이 끝나면 자동으로 종료됨

### 6.3 종료일 기준

| 항목 | 필수 | 규칙 |
| --- | --- | --- |
| 이용 종료일 | 필수 | 날짜 입력 전체 영역을 클릭하거나 Enter/Space 입력 시 브라우저 기본 달력 열기를 시도함 |
| 모집 기간 | 조건부 | 상시 모집 / 특정 기간 모집 |
| 모집 시작일 | 특정 기간 모집일 때 필수 | 모집 종료일보다 늦을 수 없음 |
| 모집 종료일 | 특정 기간 모집일 때 필수 | 모집 시작일보다 빠를 수 없고, 이용 종료일보다 늦을 수 없음 |

- 모집 기간 기본값은 `상시 모집`임
- `특정 기간 모집` 선택 시 모집 시작일·종료일 입력칸을 노출하고 활성화함
- 날짜 오류는 각 입력칸 아래에 표시함

## 7. 가입·공개 설정

### 7.1 가입 설정

| 항목 | 선택지 및 규칙 |
| --- | --- |
| 가입 인원 | 제한 있음 / 제한 없음. 기본값은 제한 없음 |
| 최대 가입 인원 | 제한 있음일 때 노출. 1~9,999명의 정수만 허용 |
| 가입 승인 방식 | 자동 승인 / 직접 승인. 기본값은 자동 승인 |

- 제한 없음 선택 시 최대 가입 인원 입력을 숨기고 비활성화함
- 가입 인원 값이 정수가 아니거나 1~9,999 범위를 벗어나면 오류를 표시함

### 7.2 공개 상태

| 선택지 | 설명 | 기본값 |
| --- | --- | --- |
| 공개 | 누구나 멤버십을 확인할 수 있음 | 아니오 |
| 일부 공개 | 링크를 통해 페이지에 접근한 사용자에게만 공개함 | 아니오 |
| 나에게만 공개 | 나에게만 공개함 | 예 |

## 8. 혜택 설정

### 8.1 기본 혜택

| 혜택 | 설정값 |
| --- | --- |
| 다이렉트 메시지 이용 권한 | 제공함 / 제공하지 않음 |
| 멤버십 배지 | 제공함 / 제공하지 않음 |

- 각 설정은 모달에서 선택함
- 아직 설정하지 않은 상태를 기본으로 표시함
- 기본 혜택 또는 추가 혜택이 하나 이상 설정되면 혜택 섹션을 완료 상태로 표시함

### 8.2 추가 혜택

| 항목 | 규칙 |
| --- | --- |
| 혜택 이름 | 필수, 최대 40자 |
| 혜택 설명 | 선택, 최대 100자 |
| 등록 | 등록 즉시 목록과 등록 개수를 갱신함 |
| 삭제 | 개별 추가 혜택을 삭제할 수 있음 |

- 빈 상태 문구: `아직 추가한 혜택이 없습니다.`
- 보조 안내 박스에 실제 혜택과 구별되는 낮은 시각적 위계로 아래 예시를 노출함

```text
등록 예시
포트폴리오 피드백 1회권
멤버십 구매 시 1회 이용할 수 있는 추가 혜택입니다.
```

## 9. 상세 소개

- 상세 소개는 선택 항목이며 등록 가능 여부에 영향을 주지 않음
- Contenteditable 기반 편집기를 제공함
- 제공 도구: 굵게, 기울임꼴, 제목, 글머리표, 번호 목록, 이미지 영역 자리 표시자
- 작성 여부에 따라 우측 요약 패널의 상태를 갱신함

## 10. 우측 요약 패널 및 등록

### 10.1 필수 완료 조건

다음 조건을 모두 만족하면 등록 버튼을 활성화합니다.

- 멤버십 운영 방식 선택
- 멤버십 이름 입력
- 한 줄 설명 입력
- 대표 이미지 선택
- 유효한 판매가 입력
- 가입일 기준 또는 혜택 이용 기준: 유효한 기본 이용 기간 입력
- 종료일 기준: 이용 종료일 입력
- 종료일 기준에서 특정 기간 모집 선택 시: 유효한 모집 시작일·종료일 입력

혜택 설정과 상세 소개는 선택 항목입니다.

### 10.2 제공 기능

- 완료율 및 미완료 필수 항목 개수를 실시간 표시함
- 미완료 항목 클릭 시 해당 섹션을 펼치고 입력 위치로 스크롤·강조함
- 현재 프로토타입의 `멤버십 등록` 버튼은 토스트 메시지 표시까지만 수행함
- 실제 저장, 상품 생성, 결제 연동, 관리자 승인 API는 범위에 포함하지 않음

## 11. 미리보기

`미리보기` 버튼을 누르면 모달에서 사용자 노출 기준의 멤버십 카드를 표시합니다.

| 표시 항목 | 반영 규칙 |
| --- | --- |
| 대표 이미지 | 선택한 이미지를 표시. 미선택 시 자리 표시자 표시 |
| 운영 방식 | 선택한 운영 방식명 표시 |
| 이름·한 줄 설명 | 입력값 또는 기본 안내 문구 표시 |
| 판매가 | 즉시 할인·첫 결제 할인 결과에 따라 원가와 적용가 표시 |
| 첫 결제 혜택 | 정액 선택 시 `첫 결제 5,000원 할인`, 정률 선택 시 `첫 결제 10% 할인` 형식으로 표시 |
| 총 할인 | 실제 원화 할인액 합계를 표시 |
| 이용 안내 | 운영 방식, 이용 기간, 갱신 방식 또는 종료일을 반영 |
| 혜택 | 설정한 기본 혜택과 추가 혜택을 태그로 표시 |
| 상세 소개 | 텍스트 일부를 최대 140자까지 표시 |

## 12. 디자인·접근성 구현

- 다크 테마, 보라색 강조색, 카드형 운영 방식 선택 UI 적용
- 각 폼 섹션은 접기/펼치기 가능한 Accordion 형태임
- 라디오 그룹에 `role="radiogroup"`, `aria-labelledby`를 적용함
- 모달에 `role="dialog"`, `aria-modal="true"`, 제목 연결용 `aria-labelledby`를 적용함
- 조건부 상세 입력 영역은 `aria-hidden`과 disabled 상태를 함께 갱신함
- 940px 이하에서 요약 패널을 상단으로 이동하고, 620px 이하에서 단일 열 레이아웃으로 전환함

## 13. 변경 이력

| 일자 | 커밋 | 변경 내용 |
| --- | --- | --- |
| 2026-09-04 | `5ed0061` | 멤버십 생성 프로토타입 최초 생성 |
| 2026-09-04 | `df0dcfa` | Cloudflare Pages 배포 트리거 추가 |
| 2026-09-04 | `17234e8` | 멤버십 중심 용어와 공개 상태를 나에게만 공개로 정렬 |
| 2026-09-04 | `d60f6e7` | 사용자 노출 기준의 멤버십 미리보기 모달 고도화 |
| 2026-09-04 | `973c97c` | 추가 혜택 영역에 등록 예시 안내 추가 |
| 2026-09-04 | `dbfde1b` | v1을 운영 방식 우선 선택형 블록 UI로 재구성 |
| 2026-09-04 | `aaacbb2` | 종료일 기준 멤버십의 종료일·모집 기간 설정 추가 |
| 2026-09-04 | `71cbe1e` | 날짜 입력 영역 전체 클릭 시 달력 열기 동작 추가 |
| 2026-09-04 | `b78e788` | 특정 기간 모집 시 날짜 입력 영역 노출 방식 정리 |
| 2026-09-04 | `ecf24b6` | 모집 기간과 이용 종료일의 날짜 선후관계 유효성 추가 |
| 2026-09-04 | `ab3ba81` | 혜택 이용 기준 멤버십 설정 흐름 추가 |
| 2026-09-04 | `7873fa7` | 혜택 이용 기준의 이용·갱신 설정을 가입일 기준과 정렬 |
| 2026-09-07 | `458793e` | 묶음 이용권을 가입일 기준·1개월·자동 갱신 조건으로 제한 |
| 2026-09-07 | `59fe09c` | 운영 방식 변경 시 작성 내용 삭제 확인 팝업 및 초기화 추가 |
| 2026-09-15 | `c58877a` | 첫 결제 할인에 정액·정률 입력 방식 추가 |
| 2026-09-15 | 미커밋 | v3의 정가·판매가 직접 입력, 결과 유형, 기존 리워드 연결 관리 및 공용 리워드 수정 안내 추가 |

## 14. 검증 현황

### 완료한 검증

- 인라인 JavaScript를 `new Function()`으로 컴파일하여 문법을 검증함
- `git diff --check`로 공백·패치 형식 오류를 확인함
- Cloudflare Pages Production 배포 목록에서 최신 커밋 `c58877a` 배포를 확인함
- 최신 배포 URL의 HTML에서 정액·정률 첫 결제 할인 UI와 계산 코드를 확인함

### 미수행 검증

- 브라우저 자동화 또는 수동 클릭 기반의 전체 인터랙션 검증
- 다양한 브라우저·모바일 실기기 UI 검증
- 실제 저장·결제·멤버십 권한 연동 검증

## 15. 후속 논의가 필요한 사항

- 혜택 이용 기준 멤버십의 실제 이용 횟수, 사용 완료 판정, 종료·갱신 정책
- 정률 할인에서 100% 할인을 허용할지와 원 단위 미만 금액의 반올림 정책
- 기본 이용 기간의 최대 120개월을 JavaScript 완료 판정에도 동일하게 강제할지
- 대표 이미지 최소 해상도와 파일 형식의 서버 측 검증 정책
- 현재 프로토타입의 등록 동작을 실제 저장 API·권한·결제 정책과 연결하는 방식

## 16. v3 개선 버전

### 16.1 범위와 제외 사항

| 항목 | 내용 |
| --- | --- |
| 대상 파일 | `membership-create-prototype/index-ver3.html` |
| 적용 방식 | v1을 변경하지 않는 별도 로컬 프로토타입임 |
| 데이터 | 브라우저 메모리의 샘플 리워드 카탈로그와 연결 상태만 사용함 |
| 제외 | 실제 저장·결제·리워드 API, DM 설정 우선순위, 리워드 원본 삭제 정책은 구현하지 않음 |

### 16.2 가격과 첫 결제 할인

- 필수 입력값은 정가와 판매가임
- 정가는 1,000원 이상 5,000,000원 이하로 입력함
- 판매가는 1,000원 이상·정가 이하·5,000,000원 이하로 입력함
- 판매가가 정가보다 높으면 오류로 표시함
- 판매가 설정 결과는 아래 계산식으로 표시함

```text
할인 금액 = 정가 - 판매가
할인율 = round(할인 금액 ÷ 정가 × 100)
```

- 판매가와 정가가 같으면 할인율·할인 금액 행을 낮은 위계로 표시함
- 첫 결제 할인은 가입일 기준 멤버십에서만 노출하며 판매가를 기준으로 적용함
  - 정률: 1~100의 정수, `floor(판매가 × 할인율 ÷ 100)`
  - 정액: 1원 이상, 판매가 이하
  - 적용 안 함
- 가격 요약에는 정가, 판매가, 정가 대비 할인율, 할인 금액, 첫 결제 할인, 첫 결제 금액을 표시함
- 우측 요약과 미리보기에도 정가·판매가·첫 결제 금액을 반영함

### 16.3 결과 유형과 이용 안내

- 가입일 기준·종료일 기준·혜택 이용 기준은 설정 기준으로 유지함
- 요약과 미리보기에서는 결과 유형을 우선 표시함
  - 정기 구독 멤버십
  - 기간 이용 멤버십
  - 기간 한정 멤버십
  - 혜택 소진 멤버십
- 자동 갱신 시 결제 시점, 동일 기간 연장, 새 이용 기간 혜택 재제공을 안내함
- 혜택 이용 기준에서는 이용 기간 종료 시 남은 혜택도 종료되며, 자동 갱신 시 남은 혜택이 이월되지 않음을 안내함
- 기본 이용 기간과 모집 기간을 별도 용어로 유지함

### 16.4 기존 리워드 기반 추가 혜택

- 추가 혜택은 별도 텍스트 객체가 아니라 기존 리워드를 현재 멤버십에 연결하는 기능임
- Drawer에서 사용 가능·사용 불가 탭을 제공함
  - 사용 가능 리워드는 여러 개를 선택해 한 번에 연결할 수 있음
  - 사용 불가 리워드는 disabled 처리하고 사유를 표시함
  - 지급 관리형 리워드는 혜택 이용 기준 멤버십에서만 연결 가능함
- `+ 새 리워드`에서 기본형·지급 관리형, 제목, 설명, 리워드 요청 설문을 설정하고 생성 즉시 현재 멤버십에 연결함
- 연결된 리워드 카드의 `⋮` 메뉴에서 아래 작업을 제공함
  - 혜택 수정
  - 위로 이동 / 아래로 이동
  - 이 멤버십에서 제거
- 다른 멤버십에서도 사용하는 리워드를 수정할 때 샘플 사용 중 멤버십 수와 공용 변경 영향을 안내함
- 제거는 연결 해제만 수행하며 리워드 원본을 삭제하지 않음
- 변경한 순서는 미리보기에도 동일하게 반영함

### 16.5 기본 혜택·워딩·변경 이력

- 기본 혜택의 선택 카드에는 제공함·제공하지 않음과 선택 상태만 표시함
- DM 설정에는 기존 메시지 수신 설정과의 적용 관계가 별도 정책 확인 대상임을 안내함
- 멤버십 생성 관련 CTA와 완료 토스트에는 `생성` 용어를 사용함
- 2026-09-15: 판매가 직접 입력·결과 유형·리워드 연결 관리·공용 리워드 수정 안내를 v3에 반영함
